function verif()
{
   let qte, categorie;
   let teste=true;
   qte=produit.qte.value;
   if(qte.length === 4 || qte.length === 6)
   {
      teste=false
   }
      alert("Verifiez vos informations.");

}